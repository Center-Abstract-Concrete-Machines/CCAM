---
const { tag } = Astro.props;

const bgColors = new Map([
    ['course', 'bg-Green'],
    ['event', 'bg-Blue'],
    ['working group', 'bg-Orange'],
    ['workshop', 'bg-Yellow'],
]);
---

<button
    class=`text-Black whitespace-nowrap px-4 py-2 ${bgColors.get(tag.toLowerCase())}`
>
    {tag}
</button>
