---
const { tag } = Astro.props;

const bgColors = new Map([
    ['course', 'bg-Green'],
    ['event', 'bg-Blue'],
    ['working group', 'bg-Orange'],
    ['workshop', 'bg-Yellow'],
]);
---

<div
    class=`text-neutral-900 text-lg whitespace-nowrap px-4 py-2 ${bgColors.get(tag.toLowerCase())}`
>
    {tag}
</div>
